---
/**
 * ROOT ROUTE REDIRECT
 * -----------------------------------------------------------------------------
 * We keep / as a simple redirect to the default language route.
 * Requirement: bilingual routing (/en, /el)
 */
return Astro.redirect("/en/");
---
